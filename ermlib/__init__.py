"""erm — Elden Ring Seamless Co-op mod manager."""
__version__ = "0.1.0"
# Sent by both HTTP clients. The contact suffix is deliberately not a URL: a
# loopback address tells whoever reads the log nothing.
USER_AGENT = f"erm/{__version__} (+local)"
